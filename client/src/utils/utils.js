export const playerJoinMessage = (id) => {
    return `Player ${id} has joined the game` 
}
    
export const createTotalChangedMessage = (total) => {
    return `Player XXX change total to ${total}`
}

export const playerLeaveMessage = (id) => {
    return `Player ${id} has left the game` 
}